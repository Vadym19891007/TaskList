function Footer() {
  return (
    <footer className="footer">
      <p>
        Tecnologies and React concepts used: React, JSX, Context, useState,
        componennt composition, conditional rendering, array methods (map,
        filter), event handling
      </p>
    </footer>
  );
}

export default Footer;
